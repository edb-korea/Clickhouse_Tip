# ClickHouse + OpenTelemetry 서버 모니터링 구축 가이드

대상 환경: ClickHouse 클러스터 1샤드 / 3레플리카, 전 노드 RHEL(RHEL/Rocky/Alma 계열, `dnf` 사용) 기준.

## 1. 아키텍처

```
[모니터링 대상 서버들 (RHEL)]
   └─ otelcol-contrib (hostmetrics + journald 수집)
        └─ clickhouseexporter
             └─ ClickHouse 클러스터 (1 shard, 3 replica, 전 노드 RHEL)
```

- 메트릭(CPU/메모리/디스크/네트워크 등)과 로그(systemd 저널)를 각 서버에서 OpenTelemetry Collector(Contrib 배포판)가 수집해 ClickHouse로 바로 적재합니다.
- `clickhouseexporter`는 OTel Collector **Contrib** 배포판에만 포함되어 있으므로 반드시 `otelcol-contrib` 바이너리를 사용해야 합니다(코어 `otelcol`에는 없음).
- 1샤드 구성이므로 Distributed 테이블(샤딩)은 필요 없고, `ReplicatedMergeTree` + `ON CLUSTER`로 3개 replica에 동일한 데이터를 복제하는 HA 구성만 맞추면 됩니다.

## 2. 사전 준비: ClickHouse 클러스터 macros 확인

`ReplicatedMergeTree`와 `ON CLUSTER`를 쓰려면 클러스터 설정(`remote_servers`)과 각 노드의 macros(`cluster`, `shard`, `replica`)가 이미 구성되어 있어야 합니다. 3대 각각에서 확인하세요.

```sql
SELECT * FROM system.macros;
-- cluster, shard 값은 3대 모두 동일해야 하고, replica 값만 노드별로 달라야 함 (예: replica_1, replica_2, replica_3)

SELECT cluster, shard_num, replica_num, host_name
FROM system.clusters
WHERE cluster = '{cluster}';
-- shard_num=1 인 행이 3개(replica_num 1,2,3) 나와야 정상
```

Keeper(ZooKeeper 또는 ClickHouse Keeper)가 살아있는지도 확인합니다.

```sql
SELECT * FROM system.zookeeper WHERE path = '/';
```

아래 DDL의 `'{cluster}'`, `'{shard}'`, `'{replica}'`는 위 매크로로 자동 치환되는 리터럴 문자열입니다. 클러스터 이름이 고정값이면 `ON CLUSTER 'my_cluster'`처럼 실제 이름으로 바꿔도 됩니다.

## 3. ClickHouse DDL

운영 환경에서는 OTel Collector가 자동으로 테이블을 생성(`create_schema: true`)하게 두지 않고, 아래처럼 직접 스키마를 만들어 인덱스/파티션/TTL/복제 방식을 통제하는 걸 권장합니다. Collector 쪽은 `create_schema: false`로 설정합니다.

DDL은 **아무 노드 1대에서만** 실행하면 됩니다. `ON CLUSTER '{cluster}'` 절이 나머지 2대에도 자동으로 배포합니다.

```sql
CREATE DATABASE IF NOT EXISTS otel ON CLUSTER '{cluster}';
```

### 3.1 로그 테이블 (systemd 저널 등)

```sql
CREATE TABLE otel.otel_logs ON CLUSTER '{cluster}'
(
    `Timestamp`          DateTime64(9) CODEC(Delta(8), ZSTD(1)),
    `TraceId`            String CODEC(ZSTD(1)),
    `SpanId`             String CODEC(ZSTD(1)),
    `TraceFlags`         UInt8,
    `SeverityText`       LowCardinality(String) CODEC(ZSTD(1)),
    `SeverityNumber`     UInt8,
    `ServiceName`        LowCardinality(String) CODEC(ZSTD(1)),
    `Body`               String CODEC(ZSTD(1)),
    `ResourceSchemaUrl`  LowCardinality(String) CODEC(ZSTD(1)),
    `ResourceAttributes` Map(LowCardinality(String), String) CODEC(ZSTD(1)),
    `ScopeSchemaUrl`     LowCardinality(String) CODEC(ZSTD(1)),
    `ScopeName`          String CODEC(ZSTD(1)),
    `ScopeVersion`       LowCardinality(String) CODEC(ZSTD(1)),
    `ScopeAttributes`    Map(LowCardinality(String), String) CODEC(ZSTD(1)),
    `LogAttributes`      Map(LowCardinality(String), String) CODEC(ZSTD(1)),
    `EventName`          String CODEC(ZSTD(1)),
    INDEX idx_trace_id TraceId TYPE bloom_filter(0.001) GRANULARITY 1,
    INDEX idx_res_attr_key mapKeys(ResourceAttributes) TYPE bloom_filter(0.01) GRANULARITY 1,
    INDEX idx_res_attr_value mapValues(ResourceAttributes) TYPE bloom_filter(0.01) GRANULARITY 1,
    INDEX idx_log_attr_key mapKeys(LogAttributes) TYPE bloom_filter(0.01) GRANULARITY 1,
    INDEX idx_log_attr_value mapValues(LogAttributes) TYPE bloom_filter(0.01) GRANULARITY 1,
    INDEX idx_body_lower lower(Body) TYPE tokenbf_v1(32768, 3, 0) GRANULARITY 8
)
ENGINE = ReplicatedMergeTree('/clickhouse/tables/{shard}/otel_logs', '{replica}')
PARTITION BY toDate(Timestamp)
ORDER BY (toStartOfFiveMinutes(Timestamp), ServiceName, Timestamp)
TTL toDateTime(Timestamp) + INTERVAL 30 DAY
SETTINGS index_granularity = 8192, ttl_only_drop_parts = 1;
```

### 3.2 게이지 메트릭 (CPU 사용률, 메모리 사용량 등 순간값)

```sql
CREATE TABLE otel.otel_metrics_gauge ON CLUSTER '{cluster}'
(
    ResourceAttributes    Map(LowCardinality(String), String) CODEC(ZSTD(1)),
    ResourceSchemaUrl     String CODEC(ZSTD(1)),
    ScopeName             String CODEC(ZSTD(1)),
    ScopeVersion          String CODEC(ZSTD(1)),
    ScopeAttributes       Map(LowCardinality(String), String) CODEC(ZSTD(1)),
    ScopeDroppedAttrCount UInt32 CODEC(ZSTD(1)),
    ScopeSchemaUrl        String CODEC(ZSTD(1)),
    ServiceName           LowCardinality(String) CODEC(ZSTD(1)),
    MetricName            String CODEC(ZSTD(1)),
    MetricDescription     String CODEC(ZSTD(1)),
    MetricUnit            String CODEC(ZSTD(1)),
    Attributes            Map(LowCardinality(String), String) CODEC(ZSTD(1)),
    StartTimeUnix         DateTime64(9) CODEC(Delta, ZSTD(1)),
    TimeUnix              DateTime64(9) CODEC(Delta, ZSTD(1)),
    Value                 Float64 CODEC(ZSTD(1)),
    Flags                 UInt32 CODEC(ZSTD(1)),
    Exemplars Nested (
        FilteredAttributes Map(LowCardinality(String), String),
        TimeUnix DateTime64(9),
        Value Float64,
        SpanId String,
        TraceId String
    ) CODEC(ZSTD(1)),
    INDEX idx_res_attr_key mapKeys(ResourceAttributes) TYPE bloom_filter(0.01) GRANULARITY 1,
    INDEX idx_res_attr_value mapValues(ResourceAttributes) TYPE bloom_filter(0.01) GRANULARITY 1,
    INDEX idx_attr_key mapKeys(Attributes) TYPE bloom_filter(0.01) GRANULARITY 1,
    INDEX idx_attr_value mapValues(Attributes) TYPE bloom_filter(0.01) GRANULARITY 1
)
ENGINE = ReplicatedMergeTree('/clickhouse/tables/{shard}/otel_metrics_gauge', '{replica}')
PARTITION BY toDate(TimeUnix)
ORDER BY (ServiceName, MetricName, Attributes, toUnixTimestamp64Nano(TimeUnix))
TTL toDateTime(TimeUnix) + INTERVAL 30 DAY
SETTINGS index_granularity = 8192, ttl_only_drop_parts = 1;
```

### 3.3 합계(카운터) 메트릭 (네트워크 누적 바이트, 디스크 IO 누적 등)

```sql
CREATE TABLE otel.otel_metrics_sum ON CLUSTER '{cluster}'
(
    ResourceAttributes     Map(LowCardinality(String), String) CODEC(ZSTD(1)),
    ResourceSchemaUrl      String CODEC(ZSTD(1)),
    ScopeName              String CODEC(ZSTD(1)),
    ScopeVersion           String CODEC(ZSTD(1)),
    ScopeAttributes        Map(LowCardinality(String), String) CODEC(ZSTD(1)),
    ScopeDroppedAttrCount  UInt32 CODEC(ZSTD(1)),
    ScopeSchemaUrl         String CODEC(ZSTD(1)),
    ServiceName            LowCardinality(String) CODEC(ZSTD(1)),
    MetricName             LowCardinality(String) CODEC(ZSTD(1)),
    MetricDescription      String CODEC(ZSTD(1)),
    MetricUnit             String CODEC(ZSTD(1)),
    Attributes             Map(LowCardinality(String), String) CODEC(ZSTD(1)),
    StartTimeUnix          DateTime CODEC(Delta, ZSTD(1)),
    TimeUnix               DateTime CODEC(Delta, ZSTD(1)),
    Value                  Float64 CODEC(ZSTD(1)),
    Flags                  UInt32 CODEC(ZSTD(1)),
    Exemplars Nested (
        FilteredAttributes Map(LowCardinality(String), String),
        TimeUnix DateTime,
        Value Float64,
        SpanId String,
        TraceId String
    ) CODEC(ZSTD(1)),
    AggregationTemporality Int32 CODEC(ZSTD(1)),
    IsMonotonic            Boolean CODEC(Delta, ZSTD(1)),
    INDEX idx_res_attr_key mapKeys(ResourceAttributes) TYPE bloom_filter(0.01) GRANULARITY 1,
    INDEX idx_res_attr_value mapValues(ResourceAttributes) TYPE bloom_filter(0.01) GRANULARITY 1,
    INDEX idx_attr_key mapKeys(Attributes) TYPE bloom_filter(0.01) GRANULARITY 1,
    INDEX idx_attr_value mapValues(Attributes) TYPE bloom_filter(0.01) GRANULARITY 1,
    INDEX idx_time_minmax TimeUnix TYPE minmax GRANULARITY 1
)
ENGINE = ReplicatedMergeTree('/clickhouse/tables/{shard}/otel_metrics_sum', '{replica}')
PARTITION BY toDate(TimeUnix)
ORDER BY (ServiceName, MetricName, toStartOfHour(TimeUnix), cityHash64(Attributes), TimeUnix)
TTL toDateTime(TimeUnix) + INTERVAL 30 DAY
SETTINGS index_granularity = 8192, ttl_only_drop_parts = 1;
```

히스토그램/서머리 메트릭이나 분산 트레이싱(`otel_traces`)이 필요해지면 같은 패턴(`ENGINE = ReplicatedMergeTree('/clickhouse/tables/{shard}/<테이블명>', '{replica}')` + `ON CLUSTER '{cluster}'`)으로 테이블만 추가하면 됩니다. 경로의 마지막 `<테이블명>` 부분만 테이블마다 겹치지 않게 다르게 주면 됩니다.

### 3.4 전용 계정 생성 (쓰기 전용)

Collector가 사용할 계정은 INSERT 권한만 부여하는 것을 권장합니다.

```sql
CREATE USER otel_writer ON CLUSTER '{cluster}' IDENTIFIED WITH sha256_password BY '<강력한_비밀번호>';
GRANT INSERT ON otel.* TO otel_writer ON CLUSTER '{cluster}';
```

## 4. OpenTelemetry Collector 설치 (RHEL 계열)

각 모니터링 대상 서버(RHEL/Rocky/Alma, `dnf` 사용)에 설치합니다.

```bash
# 최신 버전 확인: https://github.com/open-telemetry/opentelemetry-collector-releases/releases
export OTELCOL_VERSION=0.135.0   # 실제 최신 버전으로 교체

curl -LO "https://github.com/open-telemetry/opentelemetry-collector-releases/releases/download/v${OTELCOL_VERSION}/otelcol-contrib_${OTELCOL_VERSION}_linux_amd64.rpm"

# 체크섬 검증 (release 페이지의 checksums.txt와 비교)
curl -LO "https://github.com/open-telemetry/opentelemetry-collector-releases/releases/download/v${OTELCOL_VERSION}/otelcol-contrib_${OTELCOL_VERSION}_checksums.txt"
sha256sum -c --ignore-missing otelcol-contrib_${OTELCOL_VERSION}_checksums.txt

# rpm -ivh 대신 dnf install 사용 (의존성 자동 처리)
sudo dnf install -y ./otelcol-contrib_${OTELCOL_VERSION}_linux_amd64.rpm
```

패키지가 설치하는 것:

- 바이너리: `/usr/bin/otelcol-contrib`
- 설정 파일: `/etc/otelcol-contrib/config.yaml`
- systemd 유닛: `/usr/lib/systemd/system/otelcol-contrib.service`
- 실행 유저: `otelcol-contrib` (비root)

### 4.1 journald 읽기 권한

`otelcol-contrib` 서비스 계정이 systemd 저널을 읽으려면 그룹을 추가해야 합니다.

```bash
sudo usermod -a -G systemd-journal otelcol-contrib
```

### 4.2 SELinux

RHEL은 기본적으로 SELinux Enforcing인 경우가 많습니다. 이 구성(hostmetrics + journald 수집 → ClickHouse로 전송)은 아웃바운드 연결만 사용하므로 별도 정책 변경 없이 대부분 동작합니다. 문제가 있으면 다음으로 확인합니다.

```bash
getenforce
sudo ausearch -m avc -ts recent -c otelcol-contrib
```

이후 이 서버가 다른 애플리케이션의 OTLP까지 수신하는 역할을 겸하게 되면(포트 리스닝 추가 시), 포트를 SELinux 컨텍스트에 등록해야 합니다.

```bash
sudo semanage port -a -t http_port_t -p tcp 4318   # 이미 등록된 타입이면 -a 대신 -m
```

### 4.3 firewalld

지금 구성은 collector가 **아웃바운드만** 사용(ClickHouse 9000/8123 포트로 연결)하므로 모니터링 대상 서버에서 별도로 포트를 열 필요는 없습니다. ClickHouse 3개 노드 쪽에서 9000(native)/8123(HTTP) 포트가 모니터링 대상 서버들로부터 접근 가능한지만 확인하세요.

추후 이 서버가 다른 애플리케이션의 OTLP를 수신하게 되면:

```bash
sudo firewall-cmd --permanent --add-port=4317/tcp   # OTLP gRPC
sudo firewall-cmd --permanent --add-port=4318/tcp   # OTLP HTTP
sudo firewall-cmd --reload
```

## 5. Collector 설정 (`/etc/otelcol-contrib/config.yaml`)

```yaml
receivers:
  hostmetrics:
    collection_interval: 30s
    scrapers:
      cpu: {}
      load: {}
      memory: {}
      disk: {}
      filesystem: {}
      network: {}
      paging: {}
      processes: {}

  journald:
    directory: /var/log/journal

processors:
  memory_limiter:
    check_interval: 5s
    limit_mib: 512
    spike_limit_mib: 128

  resourcedetection:
    detectors: [system]
    system:
      hostname_sources: [os]

  resource:
    attributes:
      - key: service.name
        value: "host-monitor"
        action: upsert

exporters:
  clickhouse:
    # 1샤드 3레플리카 전체를 나열하면 드라이버가 그중 하나로 INSERT하고,
    # 나머지 2대는 ReplicatedMergeTree가 Keeper를 통해 자동 복제합니다.
    endpoint: "tcp://ch-node1:9000,ch-node2:9000,ch-node3:9000?dial_timeout=10s&connection_open_strategy=round_robin"
    database: otel
    username: otel_writer
    password: ${env:CLICKHOUSE_PASSWORD}
    compress: lz4
    create_schema: false      # 스키마는 3장에서 수동 생성했으므로 false
    logs_table_name: otel_logs
    metrics_tables:
      gauge:
        name: "otel_metrics_gauge"
      sum:
        name: "otel_metrics_sum"
    timeout: 5s
    sending_queue:
      enabled: true
      num_consumers: 10
      batch:
        min_size: 5000
        flush_timeout: 5s
    retry_on_failure:
      enabled: true
      initial_interval: 5s
      max_interval: 30s
      max_elapsed_time: 300s

service:
  pipelines:
    metrics:
      receivers: [hostmetrics]
      processors: [memory_limiter, resourcedetection, resource]
      exporters: [clickhouse]
    logs:
      receivers: [journald]
      processors: [memory_limiter, resourcedetection, resource]
      exporters: [clickhouse]
```

비밀번호는 파일에 평문으로 넣지 말고 systemd 환경변수 파일로 분리하는 것을 권장합니다.

```bash
sudo mkdir -p /etc/systemd/system/otelcol-contrib.service.d
sudo tee /etc/systemd/system/otelcol-contrib.service.d/override.conf <<'EOF'
[Service]
EnvironmentFile=/etc/otelcol-contrib/clickhouse.env
EOF

echo 'CLICKHOUSE_PASSWORD=<강력한_비밀번호>' | sudo tee /etc/otelcol-contrib/clickhouse.env
sudo chmod 600 /etc/otelcol-contrib/clickhouse.env
```

`clickhouseexporter`는 내부 `sending_queue.batch`로 자체 배치 처리를 하므로, 파이프라인에 별도의 `batch` 프로세서를 추가하지 마세요(위 설정에 이미 반영됨).

## 6. 실행

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now otelcol-contrib
sudo systemctl status otelcol-contrib
journalctl -u otelcol-contrib -f
```

## 7. 검증

### 7.1 데이터 적재 확인 (ClickHouse)

```sql
SELECT MetricName, Attributes, Value, TimeUnix
FROM otel.otel_metrics_gauge
ORDER BY TimeUnix DESC
LIMIT 20;

SELECT ServiceName, Body, Timestamp
FROM otel.otel_logs
ORDER BY Timestamp DESC
LIMIT 20;
```

### 7.2 복제 확인 (3레플리카 전체에 데이터가 퍼졌는지)

```sql
-- 각 노드에서 실행: is_readonly=0, active_replicas=3 이어야 정상
SELECT database, table, replica_name, is_readonly, active_replicas
FROM system.replicas
WHERE table LIKE 'otel_%';

-- 한 노드에 적재된 데이터가 다른 2개 노드에도 보이는지 count 비교
SELECT count() FROM otel.otel_logs;
```

## 8. 참고사항

- **TTL**: 예시로 30일을 넣었습니다. 보존 기간에 맞게 각 테이블 DDL의 `TTL` 값을 조정하세요.
- **인덱스 확장**: `system` 스크레이퍼 외에 컨테이너/K8s 환경이면 `resourcedetection`에 `docker`/`kubernetes` detector를 추가하고, 로그는 `filelog` 리시버로 애플리케이션 로그 파일도 함께 수집할 수 있습니다.
- **시각화**: 공식 ClickHouse Grafana 플러그인(`grafana-clickhouse-datasource`)이 이 스키마와 바로 연동됩니다.
- **트레이스 추가**: 분산 트레이싱까지 필요하면 `otel_traces` 테이블(같은 `ReplicatedMergeTree` + `ON CLUSTER` 패턴)과 `otlp` 리시버를 추가하면 됩니다.
